version: "3.8"

services:
  server:
    build: ./backend
    ports:
      - "8000:8000"

  client:
    build: ./react-board
    ports:
      - "5173:5173"
    environment:
      - VITE_WS_URL=ws://localhost:8000
    depends_on:
      - server